pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Food2ForkKMM"
include(":food2ForkAndroidApp")
include(":shared")
include(":buildsrc")
